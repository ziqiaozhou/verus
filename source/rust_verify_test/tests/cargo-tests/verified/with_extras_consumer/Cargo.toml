[package]
name = "with_extras_consumer"
version = "0.1.0"
edition = "2021"

[dependencies]
vstd = { path = "../../../../../vstd" }
with_extras_lib = { path = "../with_extras_lib" }

[package.metadata.verus]
verify = true

[workspace]
